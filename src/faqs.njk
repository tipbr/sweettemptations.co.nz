---
layout: layouts/base.njk
title: FAQs - Sweet Temptations
description: Find answers to common questions about our services, ordering, and delivery.
---

<main class="faqs-page">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    {# Page Header #}
    <header class="faqs-page__header text-center mb-12">
      <h1 class="font-accent text-4xl md:text-5xl text-[#fa7598] mb-4">
        Frequently Asked Questions
      </h1>
      <p class="text-lg text-gray-600">
        Find answers to common questions about our services, ordering, and delivery.
      </p>
    </header>

    {# FAQ List #}
    <div class="faq-list space-y-4">
      {% for faq in faqs %}
      <div class="faq-item border border-gray-200 rounded-lg overflow-hidden">
        {# Question Button #}
        <button class="faq-item__question w-full text-left px-6 py-4 bg-white hover:bg-gray-50 transition-colors flex items-center justify-between" onclick="toggleFAQ({{ faq.id }})" aria-expanded="false" aria-controls="faq-answer-{{ faq.id }}">
          <span class="font-medium text-lg text-gray-900 pr-4">
            {{ faq.question }}
          </span>
          <svg class="faq-item__icon w-5 h-5 text-[#fa7598] transition-transform duration-200 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </button>

        {# Answer #}
        <div id="faq-answer-{{ faq.id }}" class="faq-item__answer px-6 py-4 bg-gray-50 border-t border-gray-200 hidden" role="region">
          <p class="text-gray-700 leading-relaxed">{{ faq.answer }}</p>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</main>

<script>
  function toggleFAQ(id) {
    const answer = document.getElementById('faq-answer-' + id);
    const button = answer.previousElementSibling;
    const icon = button.querySelector('.faq-item__icon');
    
    if (answer.classList.contains('hidden')) {
      answer.classList.remove('hidden');
      button.setAttribute('aria-expanded', 'true');
      icon.style.transform = 'rotate(180deg)';
    } else {
      answer.classList.add('hidden');
      button.setAttribute('aria-expanded', 'false');
      icon.style.transform = 'rotate(0deg)';
    }
  }
</script>
